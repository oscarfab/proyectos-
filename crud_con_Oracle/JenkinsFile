pipeline {
    agent any

    environment {
        APP_NAME         = 'crud-oracle-app'
        DOCKER_IMAGE     = "${APP_NAME}:latest"
        PROJECT_PATH     = 'crud_con_Oracle'
        CONTAINER_NAME   = 'crud-oracle-container'
        ORACLE_CONTAINER = 'oracle-db'
        APP_PORT         = '8088'
        GIT_REPO         = 'https://github.com/oscarfab/proyectos-.git'
        GIT_BRANCH       = 'main'
        NETWORK_NAME     = 'jenkins-net'
    }

    tools {
        maven 'Maven'
        jdk 'Java'
    }

    stages {

        // 🧾 1️⃣ DESCARGAR CÓDIGO
        stage('Checkout') {
            steps {
                echo '📥 Descargando código desde GitHub...'
                checkout([
                    $class: 'GitSCM',
                    branches: [[name: "*/${GIT_BRANCH}"]],
                    userRemoteConfigs: [[url: "${GIT_REPO}"]]
                ])
            }
        }

        // 🧪 2️⃣ COMPILAR Y PROBAR CON H2
        stage('Compilar y ejecutar pruebas unitarias') {
            steps {
                script {
                    dir("${PROJECT_PATH}") {
                        echo '🧪 Ejecutando pruebas con H2 (sin Oracle)...'
                        bat 'mvn clean verify'
                    }
                }
            }
            post {
                always {
                    echo '📊 Publicando resultados de pruebas unitarias...'
                    // 📁 Publicar resultados de JUnit
                    junit allowEmptyResults: true, testResults: '**/target/surefire-reports/*.xml'

                    // 📈 Publicar cobertura con JaCoCo
                    jacoco(
                        execPattern: '**/target/jacoco.exec',
                        classPattern: '**/target/classes',
                        sourcePattern: '**/src/main/java',
                        inclusionPattern: '**/*.class',
                        exclusionPattern: '**/test/**'
                    )
                }
            }
        }

        // 🌐 3️⃣ CREAR RED DOCKER
        stage('Crear red Docker') {
            steps {
                script {
                    echo '🌐 Creando red Docker compartida...'
                    bat "docker network create ${NETWORK_NAME} || exit 0"
                }
            }
        }

        // 🗃️ 4️⃣ INICIAR CONTENEDOR ORACLE
        stage('Iniciar Oracle XE') {
            steps {
                script {
                    echo '🗃️ Iniciando contenedor de Oracle XE...'
                    bat "docker stop ${ORACLE_CONTAINER} || exit 0"
                    bat "docker rm ${ORACLE_CONTAINER} || exit 0"
                    bat """
                        docker run -d ^
                        --name ${ORACLE_CONTAINER} ^
                        --network ${NETWORK_NAME} ^
                        -p 1521:1521 ^
                        -e ORACLE_PASSWORD=oracle ^
                        gvenzl/oracle-xe:21-slim
                    """
                }
            }
        }

        // ⏳ 5️⃣ ESPERAR ORACLE
        stage('Esperar Oracle') {
            steps {
                echo '⏳ Esperando 90 segundos a que Oracle esté listo...'
                sleep(time: 90, unit: 'SECONDS')
            }
        }

        // 🏗️ 6️⃣ CONSTRUIR IMAGEN DE LA APP
        stage('Construir imagen Docker') {
            steps {
                script {
                    dir("${PROJECT_PATH}") {
                        echo '🏗️ Construyendo imagen Docker de la aplicación...'
                        bat "docker build -t ${DOCKER_IMAGE} ."
                    }
                }
            }
        }

        // 🚀 7️⃣ DESPLEGAR APP CON ORACLE
        stage('Ejecutar aplicación con Oracle') {
            steps {
                script {
                    dir("${PROJECT_PATH}") {
                        echo '🚀 Desplegando aplicación conectada a Oracle...'
                        bat "docker stop ${CONTAINER_NAME} || exit 0"
                        bat "docker rm ${CONTAINER_NAME} || exit 0"
                        bat """
                            docker run -d ^
                            --name ${CONTAINER_NAME} ^
                            --network ${NETWORK_NAME} ^
                            -p ${APP_PORT}:${APP_PORT} ^
                            -e ORACLE_HOST=${ORACLE_CONTAINER} ^
                            -e ORACLE_PORT=1521 ^
                            -e ORACLE_SERVICE=xepdb1 ^
                            -e ORACLE_USER=system ^
                            -e ORACLE_PASSWORD=oracle ^
                            ${DOCKER_IMAGE}
                        """
                    }
                }
            }
        }

        // ✅ 8️⃣ VERIFICAR DEPLOY (OPCIONAL)
        stage('Verificar despliegue') {
            steps {
                echo "🌍 Verificando aplicación en http://localhost:${APP_PORT}/swagger-ui.html"
            }
        }
    }

    post {
        always {
            echo '🧹 Limpiando contenedores temporales...'
            script {
                bat "docker stop ${ORACLE_CONTAINER} || exit 0"
                bat "docker rm ${ORACLE_CONTAINER} || exit 0"
            }
        }
        success {
            echo "✅ Pipeline completado con éxito. App disponible en http://localhost:${APP_PORT}/swagger-ui.html"
        }
        failure {
            echo "❌ Error en el pipeline. Revisa los logs para más detalles."
        }
    }
}
